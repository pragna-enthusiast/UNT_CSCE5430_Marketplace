
1/ The .mwb files contain Models of MySQL DB Schemas. They can be used by MySQL Workbench CLient to re-create the DB Schemas.

2/ Or open and execute the .sql file (which contains the generated sql code) in a query windows in workbench client.